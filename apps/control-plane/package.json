{
  "name": "@throughline/control-plane",
  "version": "0.1.0",
  "description": "Throughline control-plane: thin read/op HTTP API over the durable store.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check src"
  },
  "dependencies": {
    "@throughline/core": "workspace:*",
    "@throughline/store-sqlite": "workspace:*"
  }
}
