## What & why

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] Tests demonstrate the change (durability claims need a fault-injection or replay test, not prose)
- [ ] `pnpm lint && pnpm -r typecheck && pnpm -r test` pass locally
- [ ] Changeset added (`pnpm changeset`) — or explain why none is needed
- [ ] docs/guarantees.md updated if execution semantics changed
